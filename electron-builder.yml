productName: Crater Bible Project
artifactName: ${productName}-${os}-${arch}.${ext}
appId: "voyager.applications.crater-bible-project"
win:
  target: nsis
linux:
  target: AppImage
  synopsis: Free Alternative to Easyworship
  category: Presentation
mac:
  target: dmg
  category: public.app-category.social-networking
files: ["src/backend/build/**/*", "dist/**/*"]
extraResources: [{from: "src/assets/default", to: "store"}, {from: "src/assets/media", to: "application-assets"}]
directories:
  output: "build"
